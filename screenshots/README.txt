This folder is where a screenshot of the running dashboard (dashboard.png)
should be placed after you run the app locally with:

    streamlit run app.py

Take a screenshot of the browser window at http://localhost:8501
and save it here as "dashboard.png" to have it referenced by the README.
